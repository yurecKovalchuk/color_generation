import 'package:flutter/material.dart';

class ColorGenerationScreen extends StatelessWidget {
  const ColorGenerationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
